<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "groupGrant" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "groupGrant" %>">
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    
    <%= csrf_meta_tags %>
    </style>
  </head>

  <body>
    <%# application layout styled for Twitter Bootstrap 2.3 %>    

    <!--%= image_tag('featured.png', :alt => "groupGrant") %-->
    <%= render 'layouts/nav_bar' %>

    <main role="main">
      <%= render 'layouts/messages' %>
      <%= yield(:featured) %>
      <%= yield(:homepage) %>
      
      <div class="container">
        <div class="content">
          <%= yield %>        
          <footer>
            <div style="text-align:right; padding-top:10px; border-top: 2px solid grey">Designed and Developed by SEI 2014</div>
          </footer>
        </div>
      </div>
    </main>
  </body>
</html>

<%= javascript_tag "window._token = '#{form_authenticity_token}'" %>